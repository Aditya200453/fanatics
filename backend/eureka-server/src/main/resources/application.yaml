spring:
  application:
    name: eureka-server

server:
  port: ${SERVER_PORT:8761}

# Actuator for healthchecks (recommended for Docker)
management:
  endpoints:
    web:
      exposure:
        include: health,info
