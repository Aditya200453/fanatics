spring:
  application:
    name: appointment-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8074}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}

app:
  internal-key: ${INTERNAL_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health,info
